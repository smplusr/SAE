#pragma once

#include <vector>
#include <string>
#include <array>
#include <fstream>

#include "global.hh"


std::array <std::string, 2> stringsplit_f (std::string in_s, char in_c);
void paletset_f (std::string in_s);
